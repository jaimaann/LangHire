# Google Analytics 4 (optional — analytics disabled if absent)
VITE_GA_MEASUREMENT_ID=
# GA4 Measurement Protocol API secret (required for desktop app analytics)
VITE_GA_API_SECRET=
# Sentry DSN for crash reporting (optional — disabled if absent)
VITE_SENTRY_DSN=
# GitHub token for feedback submissions
VITE_GITHUB_TOKEN=
